# I don't think it is possible to use variables here, so it is
# hard coded to ~/.config/alacritty, and not XDG_CONFIG_HOME. ):
import:
 - ~/.config/alacritty/base.yml
 - ~/.config/alacritty/local.yml
